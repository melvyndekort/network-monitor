name: Deploy track-presence

on:
  push:
    branches: [main]
    paths:
      - 'lambdas/track_presence/**'
      - '.github/workflows/track-presence.yml'
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/deploy-lambda.yml
    secrets: inherit
    with:
      function_name: network-monitor-track-presence
      lambda_dir: lambdas/track_presence
